#include <stdio.h>

int main() {
    char ch, str[100];
    int i, found = 0;

    scanf(" %c", &ch);
    scanf("%s", str);

    for(i = 0; str[i] != '\0'; i++) {
        if(str[i] == ch) {
            found = 1;
            break;
        }
    }

    if(found)
        printf("Present\n");
    else
        printf("Not Present\n");

    return 0;
}
